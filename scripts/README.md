# scripts/

Small helpers only. No credentials.

Ideas for later:

- checklist printer for “is gh still authed?”
- scrubber that fails if `.env`-like files appear
- export a skills name list (not full private skill bodies) for inventory
